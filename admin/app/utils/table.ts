// Компактный профиль таблиц админки (design/06 §2, §6): плотные ряды и
// перенос длинных значений вместо горизонтального скролла — контент
// обязан вписываться в вьюпорт 1180px. Классы мёржатся с темой UTable
// (tailwind-merge), поэтому достаточно переопределить только вертикаль
// и whitespace.
export const denseTableUi = {
  th: 'px-3 py-2 text-xs',
  td: 'px-3 py-1.5 whitespace-normal',
} as const
